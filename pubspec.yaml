name: nour_charity_flutter
description: "Nour Charity App - Flutter version"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  shared_preferences: ^2.2.2
  cached_network_image: ^3.3.1
  url_launcher: ^6.2.5
  image_picker: ^1.1.0
  share_plus: ^9.0.0
  printing: ^5.12.0
  http: ^1.2.0
  # easy_localization: ^3.0.0    # تم تعليقها مؤقتاً
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    # - assets/translations/   # تم تعليق مجلد الترجمة مؤقتاً

  # fonts:
  #   - family: Tajawal
  #     fonts:
  #       - asset: assets/fonts/Tajawal-Regular.ttf
  #   - family: Inter
  #     fonts:
  #       - asset: assets/fonts/Inter-Regular.ttf
